import { useEffect, useRef } from 'react';
import { Grommet } from 'grommet';

import theme from './theme';

function App() {

  return (
    <Grommet theme={theme}>
      
    </Grommet>
  );
}

export default App
